var nome = 'André'

var nomeMinusculo = nome.toLocaleLowerCase()

var altura = 1.8

altura.toString() //'1.8'
altura.toFixed() // 2

function area(lado){
    return lado * lado
}


area.toString() // toda função transcrita para string

area.length() // numero de argumentos existentes

var btn = document.querySelector('.btn')

// Nomeie 3 propriedades ou métodos de strings

    var nome = 'Pedro'
    nome.at(2) // 'd'

    nome.toLowerCase()
    nome.toUpperCase()
    
// Nomeie 5 propriedades ou métodos de elementos do DOM
